# Block all incoming traffic by default
sudo ufw default deny incoming
sudo ufw default allow outgoing

# Allow specific ports (SSH, HTTPS, HTTP)
sudo ufw allow 22/tcp
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp

# Enable the firewall
sudo ufw enable

# Check the status
sudo ufw status

